# Oracle Cloud Tools Requirements
# Install with: pip install -r requirements.txt

# Oracle Cloud SDK (required for OCI tools)
oci>=2.100.0

# HTTP client (required for ticketutil.py and cloudtop.py)
requests>=2.28.0

# YAML processing (required for generate_ansible.py)
pyyaml>=6.0

# Optional: Better terminal colors (screen reader compatible)
# colorama>=0.4.6

# Development dependencies (optional)
# pytest>=7.0.0
# black>=23.0.0
# mypy>=1.0.0
